# Exercice 9  Mon Fichier modele


#!/usr/bin/env python3  # Cette indique que un fichier jenga est un script python3
# -*- coding: utf-8 -*- # Cette indique que le fichier est en UTF-8

# MonModel – Jenga Workspace #Cette ligne indique le nom du workspace Jenga
# Generated by `jenga workspace` on 2026-09-25 22:11:17 #Cette ligne indique la date de génération du fichier jenga

from Jenga import * # Cette ligne importe le module Jenga, qui contient les fonctions et classes nécessaires pour définir un workspace Jenga.

with workspace("MonModel"): # Cette ligne crée un workspace Jenga nommé "MonModel".
    configurations(['Debug', 'Release']) # Cette ligne définit les configurations de build disponibles pour le workspace: "Debug" et "Release".
    targetoses([TargetOS.WINDOWS, TargetOS.LINUX, TargetOS.MACOS]) # Cette ligne définit les systèmes d'exploitation cibles pour le workspace: Windows, Linux et macOS.
    targetarchs([TargetArch.X86_64]) # Cette ligne définit les architectures cibles pour le workspace: x86_64.
    
    # Default toolchain (auto-detected) #Cette ligne indique que la chaîne d'outils par défaut sera utilisée, et qu'elle sera détectée automatiquement.
    # usetoolchain("host-gcc") #Cette ligne spécifie une toolchain precise (Ici, "host-gcc")
    
    # Uncomment to use Unitest testing framework
    # with unitest() as u: # Cette ligne crée un bloc de configuration pour le framework de tests unitaires.
    #     u.Precompiled() # Cette ligne indique que les tests unitaires seront précompilés.
    
    # Add your projects here
    # with project("MyApp"):  #Cette ligne crée un projet nommé "MyApp".
    #     consoleapp()  #Cette ligne indique que le projet est une application console.
    #     language("C++") #Cette ligne indique que le projet est écrit en C++.
    #     cppdialect("C++17") #Cette ligne indique que le projet utilise la norme C++17.
    #     location(".") #Cette ligne indique que les fichiers sources du projet se trouvent dans le répertoire courant.
    #     files(["src/MyApp/**.cpp"]) #Cette ligne indique que tous les fichiers .cpp du projet "MyApp" se trouvent dans le répertoire "src/MyApp" et ses sous dossiers.
    #     includedirs(["src"]) #Cette ligne indique que le répertoire "src" est inclus dans les chemins de recherche des fichiers d'en-tête.
    #     objdir("%{wks.location}/Build/Obj/" # Cette ligne indique que les fichiers objets générés par le projet seront placés dans le répertoire "Build/Obj" du workspace.
    #          "%{cfg.buildcfg}-%{cfg.system}/%{prj.name}") # Cette ligne indique que les fichiers objets seront organisés par configuration de build, système d'exploitation et nom de projet.
    #     targetdir("%{wks.location}/Build/Lib/" # Cette ligne indique que les fichiers binaires générés par le projet seront placés dans le répertoire "Build/Lib" du workspace.
    #          "%{cfg.buildcfg}-%{cfg.system}")  # Cette ligne indique que les fichiers binaires seront organisés par configuration de build et système d'exploitation.
